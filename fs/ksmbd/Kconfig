# SPDX-License-Identifier: GPL-2.0-only
#
# smbfs configuration

source "fs/ksmbd/client/Kconfig"
source "fs/ksmbd/server/Kconfig"

config SMBFS
	tristate
	default y if CIFS=y || SMB_SERVER=y
	default m if CIFS=m || SMB_SERVER=m
